\documentclass[a4paper,10pt,twocolumn]{article}
\usepackage[a4paper, margin=2cm]{geometry}

\usepackage{float}

\usepackage{amsmath}
\usepackage{titlesec}
\usepackage{array}
\usepackage{colortbl}
\usepackage{xcolor}
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,      % Use colored text instead of boxes
    urlcolor=blue,        % Set link color to blue (change if needed)
    linkcolor=blue,       % Keep internal links blue as well
    citecolor=blue,       % Keep citation links blue
    breaklinks=true,      % Allow URLs to wrap properly
}

\usepackage{xstring}
\usepackage{xparse}
\usepackage{url}
\usepackage[backend=biber]{biblatex}
\addbibresource{references.bib}


\NewDocumentCommand{\code}{v}{\texttt{\detokenize{#1}}}
\newcommand{\us}{\ensuremath{\mu\text{s}}}
\newcommand{\ms}{\ensuremath{\text{ms}}}
\newcommand{\secs}{\ensuremath{\text{s}}}
\newcommand{\commit}[1]{%
  \StrLeft{#1}{6}[\shortcommit]%
  \href{https://github.com/mazunki/in5050-ass1/commit/#1}{commit \texttt{\shortcommit}}%
}


\author{\{liseej, rolfvh\}@uio.no}
\date{2025-03-02}
\title{Home exam 1 \\ {\large Codec63 Encoder on CUDA --- IN5050}}

\usepackage{graphicx}


\begin{document}
\maketitle

\section{Introduction}
This report goes through the optimization done for the Codec63 encoder which we were tasked to parallelize through the use of CUDA, mainly consisting of motion estimation and motion compensation to encode and decode one frame of a YUV video at a time.

For this assignment, we were provided with a build server and two GPU machines. The GPU machines each had their own Nvidia Quadro K2200 card. The build server was compiling our code with \code{nvcc} version 11.4. We were using rsync to push the changes to the build server, and further synced this to the GPU machine to perform testing through \code{nsys}, and we were looking at the results locally through Nsight Systems 2024.6.2.

\section{Design}
The initial code, running sequentially on the CPU, encoded the image one frame at a time, stepping through the following steps, in order: read image from file, perform motion estimation, add motion compensation, calculate quantization, reconstruct with dequantization, and finally write the encoded image to file.

For reference, the decoder will only have access to the motion vectors (whence estimation) and the residuals (from quantization), where it will run the following procedure for each frame: read motion vectors and residuals from encoded file, add motion compensation, and reconstruct the image through dequantization.

\begin{figure}[h]
  \centering
  \includegraphics[width=1.1\columnwidth]{assets/dataflow.png} 
  \caption{Dataflow diagram for Codec63 Encoder (left)/Decoder (right) The background colour of each function corresponds with its data output. The lines represent data dependencies.}
  \label{fig:dataflow}
\end{figure}

\subsection{Data dependency constraints}
In the initial code, the encoder and the decoder shared the same functions for the aforementioned steps: estimation, compensation, quantization and dequantization. It's important that the encoded frame written by the encoder shares the same format expected by the decoder's reader, and it's desirable that we maintain function compatibility between both versions for consistency; even if our main goal is to improve the encoder.

In order to optimize the code, we need to understand the relationship between functions and the data flow. For reference, we will display this information both in the form of a descriptive table (Table~\ref{tab:relevant-functions}) using the same names as used in-code, and a flowchart figure (figure~\ref{fig:dataflow}) friendlier to people.

\begin{table*}[h]
  \caption{Relevant functions}
  \renewcommand{\arraystretch}{1.3}
  \rowcolors{2}{gray!20}{white}
  % chktex-file 44
  \begin{tabular}{>{\scriptsize}p{3cm}|>{\scriptsize}p{1.5cm}|>{\scriptsize}p{1.5cm}|>{\scriptsize}p{1.5cm}|>{\scriptsize}p{7cm}}
    \hline
    \rowcolor{gray!50}
    \textbf{function} & \textbf{data in} & \textbf{data out} & \textbf{hardware} & \textbf{description} \\ 
    \hline
    \code{read_yuv} & file & \code{orig} & disk, CPU & reads the uncompressed YUV data into a buffer \\
    \code{c63_motion_estimate} & \code{orig}, \code{ref} & \code{mbs} & CUDA & finds the movement of pixels between the previous frame and the current frame \\
    \code{c63_motion_compensate} & \code{ref}, \code{mbs} & \code{predicted} & CUDA & predicts what a frame would look like (compensating for the lack of original frame data) \\
    \code{dct_quantize} & \code{orig}, \code{predicted} & \code{residuals} & CPU & applies colour correction as the difference between the predicted image and the original image, stored in the frequency domain (discrete cosine transform) and compressed through quantization \\
    \code{dequantize_idct} & \code{predicted}, \code{residuals} & \code{recons} & CPU & reconstructs the image by applying colour correction on top of the predicted image \\
    \code{write_frame} & \code{mbs}, \code{residuals} & encoded file & disk, cpu & saves the encoded data to disk with required header information for each frame \\
    \code{parse_frame} & encoded file & \code{mbs}, \code{residuals} & disk, cpu & parses an encoded frame, storing the motion vectors and the residuals into a frame buffer
  \end{tabular}
  \label{tab:relevant-functions}
\end{table*}

\section{Objectives}
The purpose of having an encoder is to reduce the file size of our image, but this is not the focus of this assignment: we are simply reusing the algorithms provided to us. Our goal, primarily, is to make the encoder run faster by the means of exploiting the parallel nature of the task. For this, we will be using CUDA, a platform and interface allowing us to execute code on multicore GPUs.

\pagebreak

\section{Optimization techniques}
Without getting into the specifics, there are two main areas which causes the encoder to be slow. The first one is the synchroneous nature of running functions in sequence, where each step has to wait for the previous one to finish. In a naïve implementation, this is necessary because we need to wait for the data to be populated before we continue.

The second cause of slowness is the fact that each individual function is slow by itself. In an initial profiling we observed that most of the time spent encoding was active CPU time spent finding the motion vectors during estimation.

Initial measurements of the precode running on the CPU tells us that the complete encoding takes roughly 122.28 \secs, where each relevant function call in the \code{encode_image} can be seen in the table below:

\begin{table}[h]
  \caption{CPU-only measurements}
  \centering
  \renewcommand{\arraystretch}{1.3}
  \rowcolors{2}{gray!20}{white}
  \begin{tabular}{l|c}
    \hline
    \rowcolor{gray!50}
    \textbf{Function} & \textbf{Execution Time} \\ 
    \hline
    Frame loading & 62.34 µs \\
    Estimation & 339.56 ms \\
    Compensation & 339.56 µs \\
    DCT+Quantization & 34.04 ms \\
    Dequantization+iDCT & 33.09 ms \\
    Frame writing & 2.04 ms \\
  \end{tabular}
  \label{tab:slow-cpu-timings}
\end{table}

The values for quantization, DCT, dequantization and frame loading + frame writing are somewhat relevant to us even if we're not targetting them for our optimization. If we have to wait for these functions anyway, it doesn't really matter how fast our CUDA code becomes. That said, upgrading hardware will lower these caps, and we shouldn't stop just looking for improvements just because our current hardware is bottlenecked by this.

\subsection{Parallelizing pixel maths}
\subsubsection{Using CUDA to parallelize tasks}
If we want to parallelize the execution of estimation on CUDA, we need to ensure that the GPU has access to the data it needs to perform calculations on. In the first iteration of motion estimation, we blindly allocated memory inside the \code{c63_motion_estimate} for both the inputs and the outputs, and synchronized the execution at the end of this function. This was a bad idea, and in hindsight it makes complete sense. The total time for encoding went up to circa 540 seconds, even with parallelization!

Our failed attempt quickly made us realize that we would benefit from reusing memory allocations, so we decided to allocate pinned memory for all our transfers before starting to encode a single frame (see \commit{f038f37ca7babe508400ba9045d74d26c6f2aafd}).

We also thought that reducing the amount of data-which-wasn't-going-to-be-used-anyway we transferred to the device would be a good idea. Additionally, because the GPU is not capable of reading host memory directly (without using mapped memory), we decided to get rid of any unnecessary indirection from the parameters sent to the kernel functions (see \commit{0581de573f7c0c71123f6a185d969055ab63231e}).

\subsubsection{Parallelizing estimation}
After having set up memory on the device, and having dealt with the required wrappers for the kernel, we can calculate estimation in parallel on the GPU instead (see \commit{8bc5079bd962fe9c7d2c7120442072aa5cb42f54} and \commit{d30b1aa11223f4b4a18fe1e1939b0ab37fd6ef79}). While simply moving the kernel code to run on the GPU by itself doesn't improve performance much, getting rid of loops inside each kernel does drastically speed up the execution.

As a note, we can avoid passing the index locations to the device, too, since each thread is aware of its own CUDA thread index, which we can treat as a free parameter. 

This parallelization, with no further changes to the flow, reduces the total time from 122 \secs\, to roughly 28 \secs. Huge!

\subsubsection{Avoiding synchronization}
Looking at the profiling results of the preivous steps, at 28 seconds, we saw that synchronization of devices was the bottleneck. To handle this, we decided to run each kernel on a stream, and only sync them afterwards. On its own, this improved the runtime by a smidge, but nothing meaningful; but it did permit us to reorder the synchronization, and only do it when required.

We would extend this asynchronous execution to memory transfers between the host and the device too, which again didn't help much on its own, but actually set us up to interleave certain CPU function calls. It was a this point where we realized we could benefit from prefetching device memory as soon as the old device memory was no longer going to be used\footnote{the code we delievered actually starts copying after starting dequantization, but this should be started immediately after compensation!}. Remember: \code{dct_quantize}, \code{dequantize_dct} and \code{write_frame} entirely use host memory, and are the last steps of each frame loop. Effectively, this means we can start loading in the next buffer to the device prior to calling these slow CPU functions asynchronously.

Prefetching the next frame by the use of a framebuffer (see \commit{0a8ef5a568f3429aac6312a84c212801112f8ae7}) reduced the total execution time from roughly 28 seconds to around 17 seconds. Again, huge!

\subsubsection{Parallelizing compensation}
After our success with parallelizing estimation, we decided we could do the same thing with compensation. We hadn't done this prior, since it wasn't really the bottleneck for performance until this point.

In our first iteration of the code, we would keep the CPU-version of this around, since the decoder also depended on this, and we didn't want to deal with refactoring the decoder yet. Eventually, we would just upgrade the decoder to support the required initialization for CUDA, since it made the code more maintainable regardless, plus it really had no downsides.

One of the benefits of running compensation on the GPU is that we no longer had to wait for the device memory of the motion vectors to return to the host, since we can now read from the same device memory addresses the estimation would write to previously. This would effectively parallelize motion compensation with the memory transfer of the motion vectors.

We didn't really observe much of a difference after optimizing compensation, albeit the kernel itself would run faster. The bottleneck at this point is mostly bandwidth transfer anyway.

\subsection{Tuning kernel code}
Even if the bottleneck at this point seems to lay at the bandwidth for memory transfer, we decided to look into making each kernel run faster on its own. Upgrading hardware could make these optimizations relevant, even if isn't showing any major benefit for the particular hardware we are testing on.

\subsection{Sum of Absolute Differences (SAD)}
Motion estimation is calculated for each macroblock (a non-overlapping 8x8 region of a frame) in the current frame. This is done by finding the best match for each macroblock in the current frame with the one in the previous frame. The best match, in this case, is the macroblock with the minimal total pixel difference.

We can make the calculation of the SAD quicker by having each thread in the warp calculate the difference between a single pair of pixels, and sum up the differences after all 64 pairs have been calculated. This makes no thread left hanging, as each warp contains 32 threads~\cite{nvidia_cuda_best_practices}.

\subsection{Improving macroblock loops}
To optimize the traversal of macroblocks and minimize memory contention, we distribute the workload across threads using a stride of \code{blockDim.x} and \code{blockDim.y}. This allows each thread to process different candidate positions in parallel, without blocking memory, improving overall efficiency. 

Additionally, using \code{atomicMin} instead of synchronizing threads each time someone wants to write their value avoids having to wait for all threads to finish.

However, further optimization could be achieved by improving memory access patterns to ensure better coalescence and warp utilization.

\subsection{Shared memory, constant memory}
In order to avoid waiting for data transfer between warps, we make use of shared memory for the SAD blocks. Shared memory is much faster than the global memory it would have used otherwise. While we can't confirm this, we suspect there might be some unreported benefit to using shared memory on top of the speed, as this frees up some space in the global memory space we could use for buffering.

We also initialize constant memory at the beginning of the program with buffer sizes ranges, widths and the sort since they are never changed, and can thus be cached between kernel launches.

\subsection{Memory types and memory access}
In an intial version of our code, we would copy the reconstructed frame into the reference frame buffer for the next frame in the pipeline, but we quickly thought that because this data already exists on the device in another location, it would be much quicker to just swap the pointers each frame. In the code, this could cause some confusion, since it's not super clear what the \code{reference} frame currently means, but the advantages of it are clear.

We also played around a bit with different types of memory allocation, such as simply mallocing on the host with \code{malloc}, and also with different categories of \code{cudaMalloc}. From what we saw, the best memory type to use in our implementation was cudaHostAllocDefault, but we do believe using cudaHostMapped would be faster after implementing \code{cudaMemPrefetchAsync}.

\section{Results}
We have profiling results for a few iterations of the code. The initial cpu-driven execution was described in Table~\ref{tab:slow-cpu-timings}. On top of this we have the following measurements:

\begin{table}[h]
  \caption{Measurements from Nsight Systems \nocite{nsight-sys}}
  \renewcommand{\arraystretch}{1.3}
  \rowcolors{2}{gray!20}{white}
  \begin{tabular}{>{\scriptsize}p{3cm}|>{\scriptsize}l|>{\scriptsize}l|>{\scriptsize}l}
    \hline
    \rowcolor{gray!50}
    \textbf{Configuration} & \textbf{Total} & \textbf{Estimation} & \textbf{Frame} \\ 
    \hline
    CPU-only & 120 s & 339 \ms & 407 \ms \\
    Redundant device allocation & 540s & N/A & N/A \\
    Synchronized kernels & 64.03 s & 139.371 \ms & 212 \ms \\
    Streamed kernels & 28.5 s & N/A & N/A \\
    Framebuffering/async memory transfer & 17.75 s & N/A & N/A \\
  \end{tabular}
  \label{tab:measurements}
\end{table}

All the measurements done here are done with CMake set to Debug mode. Using release mode easily improves these measurements by an order of magnitude.

\section{Discussion}
\subsection{Malfunctioning GPU machines}
Both of the machines provided to us (\code{in5050-2014-11} and \code{in5050-2016-10}) were down since Friday 2025-02-31 afternoon, which is when we were both wrapping up our code optimizations, and started to collect profiling summaries for this report. This consequently affect both our code delivery, as we could not test the last commits we had planned to include, and made it impossible to add any new major optimizations.

We had planned to collect profiles for each feature of optimization we have added, but this was impossible since we do not have access to an Nvidia machine when the servers are down. We do have some data points, but we can't know for certain which commits they belong to. 

Most of this report stems from the progressive profiling we have done, and the vibes we've felt from each step. Sadly, though, we did not have a chance to make a combinatorial permutation of the different approaches, nor do we have exact values for what each step of the kernel tuning added.

\subsection{Further ideas}
Our implementation is far from perfect. We have a few ideas in mind which we didn't have the opportunity to implement due to problematic hardware:
\begin{itemize}
  \item Using shuffle functions (\code{__shfl_*}) could improve sad block calculations by improving branching and looping with a warp by having each warp be aware of its index.
  \item Reusing kernel launches with new parameter values would minimize the overhead each frame.
  \item Using \code{cudaMemPrefetchAsync} might allow the GPU to handle memory better, since it knows better when it has free memory. It might be even better to calculate the optimal buffer banks and whatnot from ourside, instead. This should be tested.
  \item Allocating more space for preemptive framebuffering on the GPU might be beneficial, as it seems we aren't even close to filling the device memory.
  \item Combining the use of events for data transfers and kernel execution completion to preemptively launch memory transfers without stalling would allow for interleaving more CPU operations while we're waiting for the kernels to finish. That is, we want to remove all cases of manually synchronizing streams or devices, and have CUDA handle data dependencies by its own scheduler.
  \item We know that memory alignment is important, since copying an entire memory page from the start to its end will be quicker than starting at some offset of it, but we haven't looked into this yet.
\end{itemize}

\section{Conclusion}
We consider our task a success, but we do believe further improvements could benefit us. There is not too much to gain in our case from relying on the compiler's optimization flags, because the task is a bit more complex than what generic compiler algorithms know how to speed up due to the data dependency requirement which is in the spec of YUV and is thus our responsibility.

Most of the information related to this report can be found on GitHub (\code{mazunki/in5050-ass1}\nocite{github}) at the \code{neo} branch. You can find the specific reports from the profiler under \code{docs/report/assets/*.nsys-rep}

\printbibliography

\end{document}

